// Exporting an object containing all of the models. As there is only one model, this file is superfluous but included for consistency with other work.

module.exports = {
  Workout: require('./Workout'),
};
